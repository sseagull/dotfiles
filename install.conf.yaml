- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/Sites

- shell:
    #  - [git submodule update --init --recursive, Installing submodules]
    - echo "hey you guys"
    - command: ./test.zsh
      stdout: true
      stderr: true
